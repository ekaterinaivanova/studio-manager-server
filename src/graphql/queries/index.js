import {
  scheduledDate,
  scheduledDates
} from './ScheduledDate.query';

import {
  hall,
  halls
} from './Hall.query';

import {
  scheduledTerm,
  scheduledTerms
} from './ScheduledTerm.query';

import {
  user,
  users
} from './User.query';
module.exports = {
  scheduledDate,
  scheduledDates,
  hall,
  halls,
  scheduledTerm,
  scheduledTerms,
  user,
  users
};
